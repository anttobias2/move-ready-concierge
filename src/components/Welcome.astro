<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Move Ready Concierge — Premium Moving Coordination</title>
  <meta name="description" content="Move Ready Concierge delivers premium, stress-free moving coordination. One point of contact for movers, cleaners, haulers, and repairs." />

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary: #0f172a;
      --accent: #2563eb;
      --accent-soft: rgba(37,99,235,0.12);
      --bg: #ffffff;
      --muted: #64748b;
      --border: #e2e8f0;
      --radius: 18px;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: 'Inter', system-ui, sans-serif;
      background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
      color: var(--primary);
      line-height: 1.6;
    }

    a { text-decoration: none; color: inherit; }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.25rem;
    }

    header {
      position: sticky;
      top: 0;
      background: rgba(255,255,255,0.85);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--border);
      z-index: 1000;
    }

    .nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      height: 76px;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-weight: 700;
      letter-spacing: 0.3px;
    }

    /* .logo-mark {
      width: 40px;
      height: 40px;
      color: white;
      border-radius: 12px;
      display: grid;
      place-items: center;
      font-weight: 800;
      font-size: 0.9rem;
    } */

    nav {
      display: flex;
      gap: 1.75rem;
      align-items: center;
      font-weight: 500;
    }

    nav a:hover { color: var(--accent); }

    .btn-primary {
      background: var(--primary);
      color: white;
      padding: 0.65rem 1.2rem;
      border-radius: 12px;
      font-weight: 600;
      transition: all 0.15s ease;
      box-shadow: 0 10px 28px rgba(15,23,42,0.18);
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 36px rgba(15,23,42,0.22);
    }

    .hamburger { display: none; flex-direction: column; gap: 5px; cursor: pointer; }
    .hamburger span { width: 26px; height: 3px; background: var(--primary); border-radius: 4px; }

    @media (max-width: 768px) {
      nav {
        position: absolute;
        top: 76px;
        left: 0;
        right: 0;
        background: white;
        flex-direction: column;
        padding: 1.5rem;
        display: none;
        border-bottom: 1px solid var(--border);
      }

      nav.open { display: flex; }
      .hamburger { display: flex; }
    }

    .hero {
      padding: 6rem 0 5rem;
      text-align: center;
    }

    .hero h1 {
      font-size: clamp(2.4rem, 5vw, 3.4rem);
      margin-bottom: 1rem;
      letter-spacing: -0.02em;
    }

    .hero p {
      max-width: 720px;
      margin: 0 auto 2.5rem;
      color: var(--muted);
      font-size: 1.1rem;
    }

    .hero-cta { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

    .btn-secondary {
      border: 1px solid var(--border);
      background: white;
      padding: 0.65rem 1.2rem;
      border-radius: 12px;
      font-weight: 600;
    }

    #book {
        margin-top: 3rem;
    }

    section { 
    padding: 5rem 0; 
    scroll-margin-top: 96px
    }

    .section-title { text-align: center; margin-bottom: 3rem; }
    .section-title h2 { font-size: 2.2rem; margin-bottom: 0.5rem; }
    .section-title p { color: var(--muted); }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.75rem;
    }

    .card {
      background: white;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.75rem;
      transition: all 0.2s ease;
    }

    .card:hover {
      transform: translateY(-3px);
      box-shadow: 0 20px 40px rgba(15,23,42,0.08);
      border-color: transparent;
    }

    .steps { counter-reset: step; }

    .step {
      position: relative;
      padding-left: 3.5rem;
    }

    .step::before {
      counter-increment: step;
      content: counter(step);
      position: absolute;
      left: 0;
      top: 0;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: var(--accent-soft);
      display: grid;
      place-items: center;
      font-weight: 700;
      color: var(--accent);
    }

    .cta {
      background: linear-gradient(135deg, #0f172a, #1e293b);
      color: white;
      border-radius: var(--radius);
      padding: 5rem 2rem;
      text-align: center;
      box-shadow: 0 30px 80px rgba(15,23,42,0.35);
    }

    .cta h2 { font-size: 2.4rem; margin-bottom: 1rem; }
    .cta p { opacity: 0.9; margin-bottom: 2.5rem; }

    footer {
      padding: 2.5rem 0;
      text-align: center;
      color: var(--muted);
      font-size: 0.9rem;
    }

    html {
        scroll-behavior: smooth;
    }
  </style>
</head>
<body>

<header>
  <div class="container nav">
    <div class="logo">
      <div class="logo-mark">
        <img src="/move-ready-concierge/favicon.png" alt="Move-Ready Concierge Logo" width="80" height="80" />
      </div>
      Move Ready Concierge
    </div>

    <nav id="navLinks">
      <a href="#about">About</a>
      <a href="#process">How It Works</a>
      <a href="#services">Services</a>
      <a href="#contact">Contact</a>
      <a href="#book" class="btn-primary">Book Now</a>
    </nav>

    <div class="hamburger" id="hamburger"><span></span><span></span><span></span></div>
  </div>
</header>

<main>

  <section class="hero container">
    <h1>A Premium, Stress-Free Way to Move</h1>
    <p>
      Move Ready Concierge manages every detail of your move — coordinating movers, cleaners, haulers, and repairs —
      so you can focus on your next chapter, not logistics.
    </p>
    <div class="hero-cta">
      <a href="#book" class="btn-primary">Schedule Your Consultation</a>
      <a href="#process" class="btn-secondary">How It Works</a>
    </div>
  </section>

  <section id="about" class="container">
    <div class="section-title">
      <h2>White-Glove Move Management</h2>
      <p>We replace chaos with clarity, speed, and confidence.</p>
    </div>

    <div class="grid">
      <div class="card">
        <h3>One Point of Contact</h3>
        <p>No juggling vendors, timelines, or invoices. We handle everything through a single streamlined workflow.</p>
      </div>
      <div class="card">
        <h3>Elite Vendor Network</h3>
        <p>We partner with trusted, fully vetted movers, cleaners, haulers, and contractors.</p>
      </div>
      <div class="card">
        <h3>Precision Execution</h3>
        <p>Every detail is planned, scheduled, and managed so your move is seamless.</p>
      </div>
    </div>
  </section>

  <section id="process" class="container">
    <div class="section-title">
      <h2>How It Works</h2>
      <p>A simple, efficient three-step process.</p>
    </div>

    <div class="grid steps">
      <div class="card step">
        <h3>Consultation</h3>
        <p>We assess your needs, timeline, and priorities to build your personalized plan.</p>
      </div>
      <div class="card step">
        <h3>Coordination</h3>
        <p>We schedule and manage movers, cleaners, haulers, repairs, and logistics.</p>
      </div>
      <div class="card step">
        <h3>Execution</h3>
        <p>Your move happens smoothly, efficiently, and stress-free.</p>
      </div>
    </div>
  </section>

  <section id="services" class="container">
    <div class="section-title">
      <h2>Concierge Services</h2>
      <p>Everything required to prepare, coordinate, and complete your move.</p>
    </div>

    <div class="grid">
      <div class="card">Moving Coordination</div>
      <div class="card">Professional Cleaning</div>
      <div class="card">Junk Removal & Hauling</div>
      <div class="card">Full Project Management</div>
    </div>
  </section>

  <section id="book" class="container">
    <div class="cta">
      <h2>Move Smarter. Move Easier.</h2>
      <p>Schedule your free consultation and let our concierge team manage everything.</p>
      <a href="#contact" class="btn-primary">Book Your Consultation</a>
    </div>
  </section>

  <section id="contact" class="container">
    <div class="section-title">
      <h2>Contact</h2>
      <p>Let’s simplify your move.</p>
    </div>

    <div class="grid">
      <div class="card"><h3>Email</h3><p>hello@movereadyconcierge.com</p></div>
      <div class="card"><h3>Phone</h3><p>(555) 123-4567</p></div>
      <div class="card"><h3>Service Area</h3><p>Greater Seattle Area</p></div>
    </div>
  </section>

</main>

<footer>
  <div class="container">© <span id="year"></span> Move Ready Concierge</div>
</footer>

<script>
  const hamburger = document.getElementById('hamburger');
  const navLinks = document.getElementById('navLinks');
  const year = document.getElementById('year');

  if (hamburger && navLinks) {
    hamburger.addEventListener('click', () => {
      navLinks.classList.toggle('open');
    });
  }

  if (year) {
    year.textContent = new Date().getFullYear().toString();
  }
</script>


</body>
</html>
